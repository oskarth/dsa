#include <stdio.h>

// Fresh start. let's see how this goes.
// MAX what?

#define MAX 1000

int read(int lst[]) {
  int i, c;

  while ((c = getchar()) != EOF)
    for (i = 0; i < MAX; i++)
      scanf("%d", &lst[i]); // array name is a pointer, so no &

  return 0;
}

int write(int lst[]) {
  int i;

  for (i = 0; i < MAX; i++)
    printf("%d", lst[i]);

  return 0;
}

int main() {
  printf("a");

  //  int lst[MAX];
  //read(lst); // this seems weird...
  //write(lst);

  return 0;
}
